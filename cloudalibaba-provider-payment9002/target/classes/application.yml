server:
  port: 9002

spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.0.107:1111
        #server-addr: localhost:8848
      #由 nignx 转发到nacos集群


management:
  endpoints:
    web:
      exposure:
        include: "*"

